---
labels: ['pipeline', 'entity']
description: 'A Pipeline entity.'
---

A component for a Pipeline entity, supports serialization and de-serialization of Pipeline objects and can be used from
both backend and frontend.

```ts
const pipeline = Pipeline.from({
  name: 'hello'
});
```
';
