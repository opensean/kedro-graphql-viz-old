---
labels: ['parameter', 'entity']
description: 'A Parameter entity.'
---

A component for a Parameter entity, supports serialization and de-serialization of Parameter objects and can be used from
both backend and frontend.

```ts
const parameter = Parameter.from({
  name: 'hello',
  value: 'goodbye'
});
```
';
