---
labels: ['data catalog', 'entity']
description: 'A DataCatalog entity.'
---

A component for a DataCatalog entity, supports serialization and de-serialization of DataCatalog objects and can be used from
both backend and frontend.

```ts
const dataCatalog = DataCatalog.from({
  name: 'hello'
});
```
';
