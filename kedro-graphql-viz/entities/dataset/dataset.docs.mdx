---
labels: ['dataset', 'entity']
description: 'A Dataset entity.'
---

A component for a Dataset entity, supports serialization and de-serialization of Dataset objects and can be used from
both backend and frontend.

```ts
const dataset = Dataset.from({
  name: 'hello'
});
```
';
