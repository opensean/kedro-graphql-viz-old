---
labels: ['tag', 'entity']
description: 'A Tag entity.'
---

A component for a Tag entity, supports serialization and de-serialization of Tag objects and can be used from
both backend and frontend.

```ts
const tag = Tag.from({
  name: 'hello',
  value: 'goodbye'
});
```
';
